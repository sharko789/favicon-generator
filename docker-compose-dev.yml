services:
  webserver:
    image: nginx:latest
    restart: unless-stopped
    container_name: favicon_nginx
    ports:
      - 5000:80
    volumes:
      - ./content:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf